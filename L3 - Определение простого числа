n = int(input('Please input N: '))

def is_prime(n):
    for i in range(2, n//2):
        if n%i == 0:
            return False
    return True

is_prime(n)

while True:
    n = int(input('Please input N: '))
    print(is_prime(n))
    if input('Enter 0 if you want to stop') == '0':
        break
